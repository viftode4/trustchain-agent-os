<!-- OMX:AGENTS-INIT:MANAGED -->
<!-- Parent: ../AGENTS.md -->
# trustchain-agent-os

This AGENTS.md scopes guidance to `trustchain-agent-os`. Parent AGENTS guidance still applies unless this file narrows it for this subtree.

## Bootstrap Guardrails
- This is a lightweight scaffold generated by `omx agents-init`.
- Refresh updates the layout summary below and preserves the manual notes block.
- Keep only directory-specific guidance here; do not duplicate the root orchestration brain.

## Current Layout

### Files
- `.env`
- `.gitignore`
- `CLAUDE.md`
- `LICENSE`
- `pyproject.toml`
- `README.md`

### Subdirectories
- `.benchmarks/`
- `.github/`
- `.omc/`
- `.pytest_cache/`
- `.ruff_cache/`
- `.serena/`
- `agent_os/`
- `docs/`
- `examples/`
- `gateway/`
- `tc_frameworks/`
- `tests/`

<!-- OMX:AGENTS-INIT:MANUAL:START -->
## Local Notes
- Read `CLAUDE.md` here before making non-trivial changes; it captures adapter-layer constraints.
- Verification: `pip install -e \".[dev]\"` (or needed extras) then `python -m pytest tests/ -x -q`.
- Do not add delegation/scope/TTL logic here; it belongs in `../trustchain-py`.
- Keep framework imports lazy so optional dependencies do not break the package.
- Trust/MCP failures must degrade safely, and MCP calls require `caller_pubkey` rather than anonymous fallback.
<!-- OMX:AGENTS-INIT:MANUAL:END -->
